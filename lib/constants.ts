// Headline protocol count shown across the landing copy. Single source so the
// number can't drift between sections.
export const PROTOCOL_COUNT = 42;

// Conservative representative stablecoin APY used only for the pre-generation
// "foregone yield" framing on the idle banner (before live pool APYs load).
export const REFERENCE_STABLE_APY = 8;
